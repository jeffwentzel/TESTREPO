<plugins>
    <plugin>
      <groupId>
          <!-- {@wi.groupId_Info BRT-606 you can put a lot of 
words into the comment 
and it can go onto many
lines, but the user has 
to use a brace character
to open and close the bit
they want to display in the
Polarion traceability} -->
          org.apache.maven.plugins</groupId>
      <artifactId>maven-surefire-plugin</artifactId>
      <!--version>2.9</version>
      <configuration>
        <excludes>
          <exclude>${exclude.tests.pattern}</exclude>
        </excludes>
      </configuration>
    </plugin>
</plugins>
