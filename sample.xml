<plugins>
    <plugin>
      <groupId>
          <!-- @wi.groupId_Info BRT-606 you can put a lot of
words into the comment -->org.apache.maven.plugins</groupId>
      <artifactId>maven-surefire-plugin</artifactId>
      <!--version>2.9</version>
      <configuration>
        <excludes>
          <exclude>${exclude.tests.pattern}</exclude>
        </excludes>
      </configuration>
    </plugin>
</plugins>
